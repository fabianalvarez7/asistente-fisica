# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
.venv/
venv/
env/
ENV/
env.bak/
venv.bak/

# Environment variables
.env
.env.local
.env.*.local

# Dev data subdirs (regenerated by scripts/indexar_pdfs.py).
# data/markdown/ is the exception: it holds hand-curated content
# (e.g. formulas.md) that supplements what the PDF loader can't
# extract as text. Those files ARE committed.
data/pdfs/
data/chroma/
data/*.db
data/*.sqlite*
data/*.sqlite3
# SQLite WAL mode sidecars (created automatically when the DB is opened
# in WAL mode, which is the default for modern SQLite). Not source files;
# they're regenerated on every DB write.
data/*.db-shm
data/*.db-wal
data/*.db-journal
material-fisica/

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/
coverage.xml
*.cover
.hypothesis/

# Background job logs (e.g. logs/indexar_trabajo_energia.log)
logs/

# Type checking / linters
.mypy_cache/
.ruff_cache/
.dmypy.json
dmypy.json

# IDEs / editors
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Jupyter
.ipynb_checkpoints/
*.ipynb

# Streamlit
.streamlit/secrets.toml

# OpenCode / orchestrator local caches
.atl/
.Rhistory

# Figma frame exports: PNG reference captures of the visual identity
# designed by the project's UX designer. Used as a visual reference while
# implementing the frontend; not source files. Regenerated whenever the
# Figma file changes. The committed design source is the Figma file itself
# (shared with the designer), not these local exports.
docs/design/figma-exports/

# HF embedding model snapshot. NOT committed: the model is 448 MB which
# exceeds GitHub LFS free tier's 100 MB per-file limit. The Dockerfile
# downloads it at build time (`scripts/preparar_indice_hf.py` does the
# same for local dev). After a clone, the dev runs the script (or just
# the app once) to populate this dir.
rag/index/hf-model/
