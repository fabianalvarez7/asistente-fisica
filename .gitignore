# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
.venv/
venv/
env/
ENV/
env.bak/
venv.bak/

# Environment variables
.env
.env.local
.env.*.local

# Dev data subdirs (regenerated by scripts/indexar_pdfs.py).
# data/markdown/ is the exception: it holds hand-curated content
# (e.g. formulas.md) that supplements what the PDF loader can't
# extract as text. Those files ARE committed.
data/pdfs/
data/chroma/
data/*.db
data/*.sqlite*
data/*.sqlite3
material-fisica/

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/
coverage.xml
*.cover
.hypothesis/

# Type checking / linters
.mypy_cache/
.ruff_cache/
.dmypy.json
dmypy.json

# IDEs / editors
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Jupyter
.ipynb_checkpoints/
*.ipynb

# Streamlit
.streamlit/secrets.toml

# OpenCode / orchestrator local caches
.atl/
.Rhistory

# HF embedding model snapshot. NOT committed: the model is 448 MB which
# exceeds GitHub LFS free tier's 100 MB per-file limit. The Dockerfile
# downloads it at build time (`scripts/preparar_indice_hf.py` does the
# same for local dev). After a clone, the dev runs the script (or just
# the app once) to populate this dir.
rag/index/hf-model/
