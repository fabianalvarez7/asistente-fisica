# Dashboard-only requirements for Streamlit Community Cloud deploy.
#
# This is the lean requirements set used by the `dashboard-deploy` branch
# (Streamlit Community Cloud does not support requirements overrides —
# it always installs from `requirements.txt` at the repo root). The full
# requirements.txt (in `main`) keeps the chat dependencies (torch,
# chromadb, sentence-transformers, etc.) because the chat Space uses
# Docker and installs from the same file.
#
# What the dashboard actually imports:
#   - streamlit (UI)
#   - libsql-experimental (via rag.history._get_connection)
#   - stdlib only otherwise
#
# If you add an import to dashboard/queries.py or dashboard/app.py, add
# the dep here too.

streamlit>=1.30
libsql-experimental>=0.0.55,<1.0
