# RAG
langchain>=0.3
langchain-community>=0.3
chromadb==1.5.9
sentence-transformers>=3.0

# PDF loader: pymupdf4llm for fast Markdown extraction with inline LaTeX.
# See docs/adr/0001-pdf-loader-marker.md for the full rationale — we have
# reverted to pymupdf4llm twice (June 2026 and September 2026) due to
# marker-pdf performance problems. Marker-pdf is intentionally NOT
# installed to avoid the 3.5 GB model download.
pymupdf4llm>=0.0.17

# Chat transport
fastapi
uvicorn[standard]
openai

# Embeddings — torch se declara explícito para tener control de versión
# (MPS en Mac Apple Silicon, CUDA en Windows con GPU NVIDIA)
torch>=2.3

# History persistence
libsql-experimental>=0.0.55,<1.0

# Dashboard
streamlit>=1.30

# Utilidades
python-dotenv>=1.0
tiktoken>=0.7
