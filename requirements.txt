# RAG
langchain>=0.3
langchain-community>=0.3
chromadb==1.5.9
sentence-transformers>=3.0

# PDF OCR with LaTeX formula extraction — primary loader.
# Supersedes docs/adr/0001-pdf-loader-marker.md via the marker-pdf-loader change.
marker-pdf>=1.10

# Used by rag/topics.py to extract the sidebar syllabus (Contenidos
# Temáticos.pdf). Currently also pulled in transitively by marker-pdf,
# but pinned explicitly so a future marker-pdf upgrade can't silently
# drop it and break /topics in production.
pymupdf4llm>=1.27

# Chat transport
fastapi
uvicorn[standard]
openai

# Embeddings — torch se declara explícito para tener control de versión
# (MPS en Mac Apple Silicon, CUDA en Windows con GPU NVIDIA)
torch>=2.3

# History persistence
libsql-experimental>=0.0.55,<1.0

# Utilidades
python-dotenv>=1.0
tiktoken>=0.7
